import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

function Time(props){
  return (
    <div>
      <h1>Time Working</h1>
    </div>
  );
}

Time.propTypes = {
};

export default Time;
