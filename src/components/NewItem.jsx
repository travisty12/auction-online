import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

function NewItem(props){
  return (
    <div>
      <h1>NewItem Working</h1>
    </div>
  );
}

NewItem.propTypes = {
};

export default NewItem;
