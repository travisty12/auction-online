import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

function Create(props){
  return (
    <div>
      <h1>Create Working</h1>
    </div>
  );
}

Create.propTypes = {
};

export default Create;
