import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

function Join(props){
  return (
    <div>
      <h1>Join Working</h1>
    </div>
  );
}

Join.propTypes = {
};

export default Join;
