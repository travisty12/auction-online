import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

function Message(props){
  return (
    <div>
      <h1>Message Working</h1>
    </div>
  );
}

Message.propTypes = {
};

export default Message;
