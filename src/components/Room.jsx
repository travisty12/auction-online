import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

function Room(props){
  return (
    <div>
      <h1>Room Working</h1>
    </div>
  );
}

Room.propTypes = {
};

export default Room;
