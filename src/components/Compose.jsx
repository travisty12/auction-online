import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

function Compose(props){
  return (
    <div>
      <h1>Compose Working</h1>
    </div>
  );
}

Compose.propTypes = {
};

export default Compose;
